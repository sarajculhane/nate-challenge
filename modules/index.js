const selectButton = require('./page1')
const selectCity = require('./page2')
const {hidePopUp, completeForm} = require('./page3')

module.exports = {selectButton, selectCity, hidePopUp, completeForm}